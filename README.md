# task8-clustering-by-k-means
Implemented K-Means clustering on the Housing dataset, including preprocessing, PCA-based 2D visualization, elbow method for optimal K selection, and silhouette score evaluation.
